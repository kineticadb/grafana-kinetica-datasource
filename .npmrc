# Fail fast when the active Node version doesn't satisfy package.json "engines".
# Without this, npm only warns and the mismatch surfaces later as a confusing
# runtime error from a dependency (e.g. Array.prototype.toSorted missing on Node 18).
engine-strict=true
